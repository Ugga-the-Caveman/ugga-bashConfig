#add this into .bashrc

#uggaColorUser=
if [ -f "/etc/bash.prompt" ]
then
        source /etc/bash.prompt
fi

if [ -f "/etc/bash.aliases" ]
then
        source /etc/bash.aliases
fi



#add this to /etc/bash.bashrc

if [ -f "$HOME/.bashrc" ]
then
        source "$HOME/.bashrc"
fi

